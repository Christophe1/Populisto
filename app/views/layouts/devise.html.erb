<!DOCTYPE html>
<html>
  <head>
    <title><%= t('application.title') %></title>
     <script type="text/javascript" src="https://maps.google.com/maps/api/js?sensor=true&libraries=places"></script>
    <%= stylesheet_link_tag    "application", :media => "all" %>
    <%= javascript_include_tag "application" %>
    <%= csrf_meta_tags %>
    <!-- Le HTML5 shim, for IE6-8 support of HTML5 elements -->
    <!--[if lt IE 9]>
      <script src="http://html5shim.googlecode.com/svn/trunk/html5.js"></script>
    <![endif] don't know what this is here for-->
  </head>

  <body style="background-color: #ffffff !important;">
    <%= render :partial => 'layouts/fb_init' %>
    <!-- <div class="container"> -->
    <!-- show the signed_out_header, on the home page -->
      <%= render :partial => 'layouts/signed_out_header' %>
      <%= render :partial => 'layouts/messages' %>
      <%= render :partial => 'layouts/log_in_sign_up' %>
      <%#= render 'layouts/fb_like_button' %>
      <div class='page-content'>

        <%= content_for?(:body) ? yield(:body) : yield %>
      </div>
    </div>
  </body>
</html>
